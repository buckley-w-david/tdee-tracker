<%= form_with(model: days_meals_food) do |form| %>
  <% if days_meals_food.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(days_meals_food.errors.count, "error") %> prohibited this days_meals_food from being saved:</h2>

      <ul>
        <% days_meals_food.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
